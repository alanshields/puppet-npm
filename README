
This is a puppet module that installs node 0.3.3 from source, installs npm, and creates an npm provider.

node and npm are both installed into /home/node/opt/bin

The npm provider doesn't currently work if puppet is running as a daemon.

